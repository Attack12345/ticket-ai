# TicketAI 简历写法：PAR 结构（问题 → 方法 → 结果）

> 项目：企业智能客服工单系统（Spring Boot 3 单体，约 1.5 万行，39 个单元测试全绿）
> 使用说明：每条按 **P（针对什么问题）→ A（用了什么方法）→ R（做出什么结果）** 组织；精炼版直接贴简历，展开版用于面试准备。所有素材均为项目真实实现，可现场演示/复现。

---

## 一、精炼版（贴简历用，每条一句话：问题 → 技术/方法 → 做了什么 → 结果）

**TicketAI 企业智能客服工单系统**（Spring Boot 3 / MySQL / Redis / RocketMQ / Elasticsearch / Vue 3）
AI 增强的工单全生命周期管理：状态机驱动流转、SLA 自动升级、LLM 自动分类与 RAG 回复建议，约 1.5 万行代码，39 个单元测试全绿，GitHub 开源。

1. 针对工单流转非法操作无法统一拦截、权限约束分散的问题，使用自研状态机引擎（8 状态 × 12 事件转移矩阵），实现状态变更统一走"校验→权限→乐观锁→审计"通道，非法流转 100% 拒绝且 23 条合法转移 + 73 种非法组合测试全绿。

2. 针对延迟消息丢失导致 SLA 超时漏升级的问题，使用 RocketMQ 定时消息 + 每 5 分钟补偿扫描双保险，实现超时自动升级与消息丢失兜底，1 分钟策略实测精确到秒触发、消息丢失场景仍可升级。

3. 针对多坐席并发抢单可能超领的问题，使用 Redisson 分布式锁 + 显式乐观锁（状态+版本+无主三条件）双保险，实现数据库层面最终唯一更新，10 线程并发测试恰好 1 个成功、9 个冲突。

4. 针对 MyBatis-Plus @Version 拦截器在 version=0 时报参数注入异常导致乐观锁失效的问题，改用显式 UpdateWrapper 拼接乐观锁条件，实现冲突行为真实环境与单测一致、SQL 可审计，并发冲突统一返回 CONCURRENT_MODIFY。

5. 针对 LLM 调用成本高且不稳定可能阻塞核心流程的问题，使用自研 OpenAI 兼容客户端 + 三层降级（分类跳过/建议 503/分派规则兜底），实现 AI 增强与核心流程解耦，无 key 实测全流程照常运行。

6. 针对渠道重复提交与消息重投造成重复副作用的问题，使用唯一键/状态条件/原子操作三类幂等机制，实现六场景幂等全覆盖，重复请求实测返回同一结果。

---

## 二、展开版（面试准备，逐条讲透）

### 1. 自研状态机引擎

**P（问题）**：工单有 8 个状态、12 类事件、23 条合法转移。直接 if-else 判断会散落各处且易漏；引入 Activiti/Spring StateMachine 又过重（为 23 条转移引入 BPMN 引擎是杀鸡用牛刀），且"哪些操作需要什么权限"无法内聚。

**A（方法）**：状态/事件枚举 + 转移矩阵静态注册表（Map<State, Map<Event, Transition>>），Transition 携带目标状态与权限要求（SYSTEM 标记系统事件）。业务层 transition() 五步：状态机校验 → 权限校验 → 乐观锁更新 → 写状态日志 → 事件后置动作（并入单条 UPDATE）。

**R（结果）**：测试覆盖全部 23 条合法转移 + 96 种组合中 73 种非法组合全量断言；非法流转统一返回 409 + 明确错误码；前端按钮由后端 allowedEvents 驱动，零硬编码。

**追问应对**："为什么自研不用框架？"——复杂度匹配（22 条转移用框架是杀鸡用牛刀，且权限/审计需求内聚在转移定义里）；可控性（错误信息、审计钩子、乐观锁全部可控）；可测试性（矩阵可全量枚举测试）。生产场景流程复杂到需要可视化建模时我会上 Activiti——这是有意识的选型边界。

### 2. SLA 超时引擎（延迟消息 + 补偿扫描）

**P（问题）**：每张工单按优先级挂 SLA 策略（如紧急 15 分钟响应/4 小时解决），超时必须自动升级。但延迟消息会丢（MQ 故障、消费失败重试耗尽），只靠消息必然漏升级。

**A（方法）**：双保险。主链路：建单时按策略计算截止时间，投递 RocketMQ 定时消息（setDeliverTimeMs，broker 时间轮），到点消费者检查"是否已响应/已解决"，未达成则标记超时并触发状态机 TIMEOUT_ESCALATE（SYSTEM 事件，事件解耦避免服务循环依赖）。兜底链路：每 5 分钟补偿扫描，捞"超时未升级"记录补升级。

**R（结果）**：1 分钟策略实测——工单创建后整整 60 秒延迟消息到达并完成升级（日志时间可验证）；消息丢失场景补偿扫描仍能升级；升级动作落审计日志；消费幂等（已升级消息重复到达直接忽略）。

**追问应对**："延迟消息丢了怎么办？"——补偿扫描就是答案，这是工程上"主链路 + 兜底"的标准模式。"为什么不用 Redis 过期键？"——Redis 过期回调不可靠且本机 3.0 版本限制，MQ 定时消息 + 数据库扫描兜底更稳。

### 3. 并发抢单（锁 + 乐观锁双保险）

**P（问题）**：多坐席同时抢同一张待分派工单，必须恰好一人成功。只加分布式锁存在两个缺口：锁自动过期时任务未完成、unlock 先于事务提交导致下一个持锁者读到旧数据。

**A（方法）**：Redisson 分布式锁（lease 10s/等待 3s）减少竞争，锁内执行 UPDATE ticket SET status=处理中, agent_id=?, version=version+1 WHERE id=? AND status=待分派 AND agent_id IS NULL AND version=?——状态+无主+版本三条件，影响行数为 0 即抢单失败（乐观锁兜底锁的时序窗口）。

**R（结果）**：10 线程并发测试恰好 1 个成功、9 个 CONCURRENT_MODIFY；坐席负载实时计数。

**追问应对**："锁不是够了吗为什么要乐观锁？"——锁有自动过期和释放时序问题，乐观锁的版本条件保证数据库层面最终只允许一个更新成功。"Redis 锁过期了怎么办？"——Redisson 看门狗自动续期 + 乐观锁兜底。

### 4. 乐观锁框架缺陷绕过

**P（问题）**：需求是状态变更必须乐观锁（WHERE version + SET version+1）。但 MyBatis-Plus 3.5.17 的 @Version 拦截器在实体 version=0 时抛 `MP_OPTLOCK_VERSION_ORIGINAL not found`（实测定位，非文档行为）；且 LambdaUpdateWrapper 的 lambda 解析依赖 MP 运行时缓存，纯单元测试环境不可用。

**A（方法）**：transition 全部改用字符串 UpdateWrapper 显式拼接乐观锁条件，后置动作并入同一条 UPDATE（原子性更好）；保留 @Version 注解但不再依赖其拦截器。

**R（结果）**：并发冲突在真实环境与单测环境行为一致（UPDATE 影响 0 行 → CONCURRENT_MODIFY）；SQL 完全可审计。

**追问应对**：这是"框架缺陷 vs 可控性"的真实案例，能讲出具体报错信息和排查过程（先被吞异常、只看到 NPE、逐层定位到拦截器参数注入），体现调试能力。

### 5. AI 可降级架构

**P（问题）**：LLM 自动分类、RAG 回复建议、AI 分派三场景，LLM 有成本（每次调用花钱）且不可靠（超时/限流/挂掉），不能影响核心流程。

**A（方法）**：三场景全部"建议"定位。分类：异步执行 + Redis SETNX 防重复调用（10 分钟 TTL）；回复建议：工单向量化 → ES 召回相似已解决工单 top3 + 知识库 top3 → prompt 生成草稿（500 字截断 + 引用列表）；分派：返回 null 时策略工厂自动降级到规则策略。每次调用写 ai_usage_log（token/延迟/成功失败）。

**R（结果）**：无 key 实测——建单/分派/全流程不受影响，分类静默跳过，建议接口返回 503 明确错误码；三层降级表（分类→跳过、建议→503、分派→规则兜底）。

**追问应对**："为什么不用 Spring AI？"——三个场景只需要 chat + embeddings 两个端点，自研 200 行可控（超时/重试/JSON 校验/降级逻辑显式可见），且任何 OpenAI 兼容厂商可换；Spring AI 抽象层把细节包住。生产上场景复杂到多模型路由再上框架。

### 6. 六场景幂等设计

**P（问题）**：渠道重复提交、MQ 消息重投、并发刷新等场景会造成重复建单、重复升级、重复扣费（LLM）。

**A（方法）**：按副作用类型分三类落地——唯一键（渠道建单 message_no 唯一约束，冲突查回已有工单）；状态条件（SLA 消费查 escalation_triggered、补偿扫描条件本身幂等、状态机+乐观锁天然拒绝重复事件）；原子操作（LLM 调用 SETNX、refresh 旋转 Lua compare-and-delete）。

**R（结果）**：渠道重复 messageNo 两次请求返回同一工单（实测）；并发刷新旧 token 立即失效（实测 401）；六场景全部有测试覆盖。

**追问应对**：能按"副作用类型 → 机制"分类讲，而不是只会背"用唯一键"。

---

## 三、数字速查卡

| 指标 | 数值 | 怎么来的 |
|---|---|---|
| 状态转移 | 23 条合法 + 73 条非法全量断言 | StateMachineTest 全量组合遍历 |
| 并发抢单 | 10 线程恰好 1 成功 9 冲突 | TicketClaimConcurrentTest |
| 单元测试 | 39 个全绿 | mvn clean test |
| SLA 实测 | 1 分钟策略精确到秒触发升级 | 日志时间戳可验证 |
| 演示数据 | 113 张工单 / 21 篇知识库 / 17 张超时升级样本 | demo_data.sql 一键导入 |
| SLA 按时率 | 61.5%（演示数据） | 看板实时计算 |
| 幂等场景 | 6 类 | 渠道/SLA/补偿/ES/LLM/refresh |

## 四、演示脚本（现场验证用）

1. 启动后端（8090）+ 前端（5173），登录 admin/Admin@12345
2. 看板页：状态分布 + SLA 按时率实时数据
3. 工单详情：时间线看 TIMEOUT_ESCALATE 系统事件（演示数据里有 17 张升级单）
4. 把 SLA 策略响应时限改成 1 分钟 → 建一张紧急工单 → 60 秒后自动升级（现场演示延迟消息）
5. 知识库检索 + 工单详情 AI 建议抽屉（配 key 后演示 RAG 草稿）
